diff --git a/src/od.c b/src/od.c
index f893729..76db57c 100644
--- a/src/od.c
+++ b/src/od.c
@@ -1006,6 +1006,11 @@ open_next_file (void)
     }
   while (in_stream == nullptr);
 
+#ifdef __MVS__
+  if (!isatty(fileno(in_stream)))
+  __disableautocvt(fileno(in_stream));
+#endif
+
   if (0 <= end_offset && !flag_dump_strings)
     setvbuf (in_stream, nullptr, _IONBF, 0);
 
